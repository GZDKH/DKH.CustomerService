{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gzdkh.com/schemas/contracts-version.schema.json",
  "title": "GZDKH Contracts Version Configuration",
  "description": "Version and publication settings for a single Contracts package",
  "type": "object",
  "required": ["version", "publish", "forceRepublish", "delete"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[a-zA-Z0-9.-]+)?$",
      "description": "SemVer version of the package (e.g., 1.0.0, 1.2.3-beta)"
    },
    "publish": {
      "type": "boolean",
      "description": "Whether to publish this package to GitLab Package Registry"
    },
    "forceRepublish": {
      "type": "boolean",
      "description": "Force republish even if package already exists (overwrite existing version)"
    },
    "delete": {
      "type": "boolean",
      "description": "Mark package for deletion from GitLab Package Registry"
    }
  },
  "additionalProperties": false
}
